# Keeps line endings and charset predictable between machines (closer dev/prod parity for text files).
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.{html,css,js,json,txt,md}]
indent_style = space
indent_size = 2
